# 출처와 파생 관계

이 문서는 digi-mon의 교육과정 자료가 어디서 왔고, 현재 어떤 자료를 실행
입력으로 사용하며, 어떤 권리 검토가 남았는지 기록한다.

## 현재 실행 코퍼스

digi-mon은 다른 저장소를 설치하거나 읽지 않는다. 설치, 시험, 전체 검증,
명령줄 도구와 HTTP 서버는 다음 저장소 내부 파일만 사용한다.

- `data/spine/standards.json`: 248개 성취기준 내부 스파인
- `reference/[별책5] 국어과 교육과정.md`
- `reference/[별책8] 수학과 교육과정.md`
- `reference/[별책14] 영어과 교육과정.md`

`src/ontology/corpus-pin.mjs`는 네 파일의 LF 정규화 SHA-256과 성취기준 수를
고정한다. 로더는 다음 조건을 모두 확인한 뒤에만 코퍼스를 반환한다.

1. 네 파일의 해시가 고정값과 일치한다.
2. 내부 스파인의 자료 구조와 선언한 개수가 실제 레코드와 일치한다.
3. 성취기준 코드, 내부 식별자와 명세 식별자에 중복이 없다.
4. 교과·학년군과 성취기준 코드가 서로 맞는다.
5. 내부 스파인의 248개 코드가 세 공식 별책에서 추출한 고유 코드와 정확히
   일치한다.

`KELM_DIR`, 형제 저장소 탐색, 외부 체크아웃과 원격 내려받기는 사용하지 않는다.
CI도 digi-mon 저장소 하나만 체크아웃한다.

## 내부 스파인의 역사적 계보

현재 `data/spine/standards.json`의 최초 버전은
Korean Elementary Learning Map의 `kr-full-depth-v0.4` 자료를 읽어 만든 파생
스냅샷이다.

- 당시 확인한 저장소: `github.com/DECK6/korean-elementary-learning-map`
- 저작자 표시: DECK
- 당시 확인한 커밋:
  `3ef0563084aa2a9baaa47da2c1ec0ebf5d7edc5c`
- 당시 사용한 자료:
  `curriculum-standards.json`, `topics.json`, `dependencies.json`,
  `clusters.json`

이 기록은 현재 실행 의존성이 아니라 과거 데이터 계보다. digi-mon은 더 이상
해당 저장소의 변경을 추적하거나 자동으로 가져오지 않는다. 과거에 가져온
성취기준 보조 정보와 주제 정렬은 고정된 내부 스냅샷으로만 남아 있으며, 로드할
때 `provenance`와 `alignment`라는 digi-mon 내부 계약으로 변환된다.

출처 표시를 지우거나 자체 저작 자료로 바꾸어 적는 것은 독립화가 아니다.
기술적으로 독립 실행되더라도 기존 스냅샷의 권리 계보는 보존해야 한다.

## 직접 대조하는 공식 교육과정

2026-08-06에 교육부 고시 제2022-33호의 국어·수학·영어 자료를 직접 검토했다.

- 공식 고시:
  [NCIC 2022 개정 초·중등학교 교육과정 고시 안내](https://ncic.go.kr/board/B0033.cs?act=read&bwrId=2105&pageIndex=2&pageUnit=15)
- 국어: 교육부 고시 제2022-33호 `[별책 5]`
- 수학: 교육부 고시 제2022-33호 `[별책 8]`
- 영어: 교육부 고시 제2022-33호 `[별책 14]`
- 접근일: 2026-08-06

저장소의 세 Markdown은 성취기준 코드 대조와 영어 공식 어휘 추출에 사용한다.
원본 HWP·PDF 편집물은 실행 코퍼스에 포함하지 않는다.

영어 `[별표 3]`의 공식 초등 권장 대표 표제어 800개와 병합 후보 1,252개의
파일 해시는 `data/curriculum/english-official-vocabulary.json`에 기록한다.
자세한 경계는 `docs/assets/english-vocabulary-provenance.md`에 있다.

## 저장소가 직접 저작한 자료

다음 자료는 외부 학습지도 저장소에서 가져오지 않았다.

- `src/`, `bin/`, `tools/`의 코드
- 문항 생성기와 채점·검산 로직
- 사실 표의 문장과 낱말
- 문장·어휘 시드 목록
- 학습 목표, 원리·공식·도움말과 교사용 검토 후보
- 선수 관계와 학습 게이트 정책
- 제품·설계·검토 문서

## 생성 산출물

`npm run verify`는 내부 코퍼스와 저장소 코드를 사용해 다음 자료를 검사하거나
다시 만든다.

- `data/coverage/coverage.json`
- `data/curriculum/prerequisites.json`
- `data/audit/*.json`
- `docs/review/*.md`

`data/spine/standards.json`은 더 이상 외부 저장소에서 재생성하지 않는다.
`bin/build-spine.mjs`는 고정 해시, 자료 구조와 공식 별책 코드 일치를 검사하는
검증 명령이다.

## 라이선스 검토 경계

Korean Elementary Learning Map의 당시 라이선스 메타데이터에는 서로 다른 선언이
있었다.

| 파일 | 당시 선언 |
|---|---|
| `package.json` | `(ODbL-1.0 AND CC-BY-SA-4.0)` |
| `LICENSE` | MIT |
| `NOTICE.md` | MIT |
| `CITATION.cff` | MIT와 별도 재사용 단서 |

따라서 기존 스파인이 완전한 자체 저작 자료가 되었다고 주장할 수 없다. 이 충돌이
해소되거나, 공식 교육과정 자료만으로 스파인을 새로 구축해 과거 파생 필드를
제거하기 전까지 다음을 지킨다.

- 기존 스파인을 자체 저작 데이터로 재표시하지 않는다.
- 역사적 저작자 표시를 보존한다.
- 파생 스파인의 공개 배포 조건이 확정됐다고 주장하지 않는다.
- 공개 릴리스 전에는 별도의 법률·권리 검토를 거친다.

기술적 독립 실행과 데이터 권리의 독립은 서로 다른 문제다.

## 코퍼스 변경 절차

1. 공식 별책 또는 내부 스파인 변경 근거를 기록한다.
2. 성취기준 코드·교과·학년군·정렬 변화의 영향을 검토한다.
3. `src/ontology/corpus-pin.mjs`의 개정과 해시를 의도적으로 갱신한다.
4. 관련 자료 계약, 시험과 문서를 함께 갱신한다.
5. `npm test`, `npm run verify`, 산출물 최신성 검사를 통과시킨다.

파일이 바뀌었다는 이유만으로 새 해시를 자동 승인하지 않는다.
