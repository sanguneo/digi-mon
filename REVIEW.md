# digi-mon 검토 브리프

이 문서만 읽고 검토를 시작할 수 있게 쓴 것이다. 측정치는 모두 `npm run verify` 실행 결과이며
추정값이 아니다. **반박해야 할 판단**과 **반박하면 안 되는 사실**을 구분해 적었다.

- 저장소: `git@github.com:sanguneo/digi-mon.git`
- 런타임: Node 24, ESM, **프로덕션 의존성 0개** · 검증용 dev dependency `ajv`
- 실행: `npm run verify` (열여덟 게이트, 전부 통과 시 exit 0)
- 이 문서 자체가 `check-review-doc` 게이트의 검사 대상이다. 수치가 낡으면 `verify`가 실패한다.
- 검토 이력은 §16에 짧게 있다. 이번이 5차다.

---

## 1. 이게 무엇인가

**2022 개정 대한민국 초등 교육과정에 정렬된 학습지 문항 출제 엔진.** 클라이언트가 아니다.
HTTP API로 문항·학습지·채점을 제공하고, 인쇄·PDF·화면 렌더는 범위에서 **영구 제외**했다.

핵심 설계는 세 가지다.

1. **결정성** — `seed` 하나로 같은 학습지를 바이트 단위로 재생성한다.
2. **무상태 채점** — 채점 시 서버가 학습지를 재생성해 대조하므로 **DB가 없다**.
3. **검산 필수** — 모든 생성기는 `verify(params, answer)`를 반드시 갖는다. 없으면 등록이 거부된다.
   검산이 실패하면 문항이 학습지에 나가지 않는다. 답이 틀린 수학 문항은 최악의 실패다.

상위 데이터는 자매 저장소 `korean-elementary-learning-map`(DECK6, 라이선스 메타데이터 충돌 미해결)의 `kr-full-depth-v0.4`를
읽기 전용으로 쓴다. LF 정규화 후 SHA-256으로 무결성을 확인한다(Windows 체크아웃이 CRLF라서 필요).

---

## 2. 측정된 현재 상태

```
자동채점 커버리지   150/194 성취기준 (77.3%)
문항 생성 가능      151 성취기준
명시 승인 주제 정렬 119/248 · 코드 기반 검토 후보 151/248 assesses 주제
자동채점 불가 분류   54 성취기준
전체                248 성취기준 (수학 121 · 국어 87 · 영어 40)

생성기 193개
verify-generators      문항 생성 검산 실패 0건 · SVG 렌더 37,985건
mutation-test          틀린 답 21,603개 주입 → 통과 0개 · 무력한 검산 0개
check-korean           문면 73,676건 → 조사·표기 위반 0건
check-vocabulary       문항 2,460개 / 낱말 3,547건 → 학년 어휘 위반 0건
check-fact-tables      사실 표 6종 114짝 → 구조 문제 0건
check-difficulty       numeric 120 / single 26 / categorical 47 · 평평 0 · 축 불일치 0
check-difficulty-notes categorical 47개 → 노트와 코드 불일치 0건
check-capacity         포화 100/193 · 관측 문항 용량 117,890개 · 상한 미신고 0건
check-prerequisites    성취기준 121/121 · 간선 158 (학년군 넘김 54) · 순환·역행 0
```

### 교과별

| 교과 | 자동채점 | 생성기 | 전략 | 영역별 |
|---|---:|---:|---|---|
| 수학 | **118/120** | 152 | parametric | 수와 연산 42/42 · 변화와 관계 10/10 · 도형과 측정 54/56 · 자료와 가능성 12/12 |
| 국어 | 23/49 | 29 | mixed | **문법 12/12** · 읽기 3/11 · 문학 2/10 · 쓰기 4/6 · 듣기말하기 2/5 · 매체 0/4 |
| 영어 | 9/25 | 12 | mixed | 이해 2/12 · 표현 7/13 |

**분모가 248이 아닌 이유:** 자동채점이 원리적으로 불가능한 54개를 뺐다. 자산이 없어서 못 하는
것과 정답 대조가 성립하지 않는 것은 다르다. 섞어 세면 자산을 다 갖춰도 100%가 되지 않는다.

- **수학 1개** — `[2수03-04]` 도형 **그리기**. 작도 수행이라 문항은 생성하고 채점 기준만 사람에게 넘긴다.
- **국어 38개** — 태도·평가 2 + 태도·흥미·성찰 11 · 글·자료 산출 11 · 음성·상호작용 14.
- **영어 15개** — 태도 8 · 음성 산출 4 · 창의 수행 2 · 개방 산출 1. 4차 재분류로 5개가 늘었다.

---

## 3. 열여덟 게이트 — 각각 무엇을 왜 잡는가

`npm run verify`가 이 순서로 돈다. 각 게이트는 **실제로 버그를 잡은 뒤에** 만들어졌다.
괄호 안은 그 게이트가 처음 실행됐을 때 적발한 건수다.

| # | 게이트 | 무엇을 검사하는가 | 왜 있는가 |
|---|---|---|---|
| 1 | `bin/build-spine.mjs` | 성취기준 코드를 파싱해 학년군·영역·순번을 도출하고 업스트림 기록값과 대조 (248/248 일치) | 코드 체계를 신뢰하려면 대조가 필요하다 |
| 2 | `bin/audit-ontology.mjs` | 상위 온톨로지의 텍스트 품질 측정, 교과별 사용 가능 앵커 판정 | §4 |
| 3 | `tools/check-boundaries.mjs` | 생성기가 온톨로지 주제 텍스트를 참조하는지 / 엔진에 PDF·DOM·인쇄 코드가 유입됐는지 | 결정을 주석으로 남기면 나중에 다시 끌어온다 |
| 4 | `bin/verify-generators.mjs` | 생성기×난이도×시드 대량 생성, 문항 계약 검증, `verify()` 호출, SVG well-formedness | 답이 틀린 문항이 나가는 걸 막는 1차 방어 |
| 5 | `tools/mutation-test.mjs` | **정답을 일부러 틀리게 바꿔 `verify()`에 넣는다.** 틀린 답이 통과하면 그 검산은 무력하다 (**5개 적발**) | §5 — 가장 중요한 게이트 |
| 6 | `tools/check-korean.mjs` | 숫자·단위·단위명사 뒤 조사, 조사 중복, 미치환 흔적, 공백 (**656건 적발**, 로/으로 추가로 **222건 더**) | §6 |
| 7 | `tools/check-prerequisites.mjs` | 선수 관계 그래프의 누락·허깨비 참조·순환·학년군 역행 | DAG가 아니면 복습 순서가 무한 루프한다 |
| 8 | `tools/check-vocabulary.mjs` | 국어·영어 문항의 학습 대상 낱말이 학년군 어휘 목록 안에 있는지 (**722건 적발**) | "3학년 수준으로 써라"는 지시로는 통제되지 않는다 |
| 9 | `tools/check-asset-requirements.mjs` | **미충족 기준이 무엇 때문에 막혔는지 밝히는지.** 조달 성격·필요 자산을 성취기준마다 요구한다 (**3건 적발**) | §14 — 문서가 없는 것을 있다고 했다 |
| 10 | `tools/check-vocab-selfapproval.mjs` | **어휘 목록이 자산을 자기승인하는지.** 목록 안에서 `토큰 = 다른 토큰 + 조사` 꼴을 찾는다 (**1건 적발**) | §8 — 4차 검토가 지적한 구멍 |
| 11 | `tools/check-fact-tables.mjs` | 높임말·반대말·방언·맞춤법처럼 **정답이 사실인 표**의 구조. 한 답이 두 표제어에 걸치는지, 방향이 꼬였는지 | §5 — 검산이 못 닿는 영역 |
| 12 | `tools/check-difficulty.mjs` | 난이도 축 선언과 실제 행동. numeric은 계산 크기 측정, single은 난이도로 결과가 갈리지 않는지 런타임 확인 (**5개 적발**) | §7 |
| 13 | `tools/check-difficulty-notes.mjs` | `difficultyNote`가 말한 난이도가 지원되는지, categorical인데 난이도가 문항을 전혀 바꾸지 않는지 (**3개 적발**) | §7 — 3차 검토가 지적한 공백 |
| 14 | `tools/check-capacity.mjs` | 표본을 두 배로 늘려 포화를 재고, 상한이 낮은데 이유를 밝히지 않은 생성기를 잡는다 | §7 |
| 15 | `tools/check-trim.mjs` | 계산 결과 표기(`157.00` vs `157`)가 채점에서 오답 처리되는지 | §5 |
| 16 | `tools/check-review-doc.mjs` | **이 문서의 수치가 산출물과 맞는지.** 게이트 파일이 `verify` 체인에 실제로 등록됐는지, 게이트 수 주장이 맞는지 | 문서는 게이트가 갱신해 주지 않는다. §16 |

체인은 21단계다 — 게이트 18개 + 산출물 생성 3개(`export-review-tables`). 산출물 생성기는
실패할 수 없으므로 게이트로 세지 않는다.

---

## 4. 상위 온톨로지를 어디까지 믿는가

`data/audit/ontology-audit.json`에 측정치가 있다.

| 교과 | 쓸 수 있는 앵커 | 주제 텍스트 | 선수 그래프 |
|---|---|---|---|
| 수학 | `module` (121/121) | **index-only** | **index-only** |
| 국어 | `summary` (87/87 고유) | index-only | reviewable-candidate |
| 영어 | `summary` (40/40 고유) | index-only | reviewable-candidate |

- **주제 텍스트는 보일러플레이트다.** 수학 363개 주제의 `description`·`assessmentPrompt`가
  **고유 문장틀 3개**다. "네 자리 이하의 수"와 "입체도형의 겉넓이와 부피"의 학습 내용 기술이
  글자 그대로 같고 소주제명만 치환된다. 생성기가 이 필드를 참조하는 것을 게이트로 금지했다.
- **성취기준 `summary`는 다르다.** 국어 87/87, 영어 40/40 전부 고유한 내용 라벨이다.
  수학은 이 필드가 문장틀 2개라 못 쓰는 대신 `module`이 121/121 있다.
  **앵커가 교과마다 다른 필드에 있다.**
- **상위 선수 그래프는 못 쓴다.** 수학 359개 간선 중 67%가 같은 성취기준 안의
  concept→representation→application 순서고, 영역을 넘는 간선이 **0개**, 학년군을 넘는 간선이
  8개다. `분수 → 약분 → 분수의 곱셈` 같은 실제 위계를 담지 못한다.

### 여기서 저작자가 두 번 틀렸다 — 같은 함정을 볼 것

1. `topics.json`만 감사하고 "국어는 앵커가 없다 → 0/87 원리적 불가"로 결론했다. 성취기준
   `summary`를 보지 않았다. 이후 문법 12개가 규칙 기반으로 열렸다.
2. 영어도 성취기준 40개를 **읽지 않고** "전부 불가"로 단정했다. 실제로 읽으니 문자 인식 3개는
   자산 없이 되고 10개는 애초에 자동채점 불가였다. `0/40`이 `3/30`이 됐다.

두 번 다 **"안 읽고 단정"**이 원인이다. 국어 73개는 2차 이후, 영어 27개는 4차 검토에서
전수 재분류를 거쳤다(§16). 4차에서 5개가 원리적 불가로 확정됐다 — 소리 내어 읽기 2,
공감하며 듣기 2, 감정·의견 표현 1. 모두 국어 선례와 같은 분류다.

---

## 5. 검산 설계 — 가장 중요한 부분

### `verify()`는 독립 경로여야 한다

생성 방향과 반대 방향으로 답을 되짚는다. 역연산·불변식·문자열 역파싱을 쓴다.

```js
// 좋다: 답에 나누는 수를 곱해 나누어지는 수가 되는지 본다
verify({ a, b }, answer) {
  const parsed = parseFractionText(answer.value);
  const back = multiplyFractions(parsed, b);
  return back.n === reduceFraction(a).n && back.d === reduceFraction(a).d;
}

// 나쁘다: params 에 담아 둔 계산 결과를 되읽는다 — answer 를 보지 않는다
verify({ result, b }, answer) {
  return subFractions(result, b).n === a.n;   // 늘 참. 아무것도 검증하지 않는다.
}
```

### mutation-test가 무력한 검산 6개를 적발했다

`verify()`가 **있다는 것**과 **무언가를 잡는다는 것**은 다르다.

- `[4수01-15]` 분모가 같은 분수의 덧뺄 — **100% 무력** (`answer`를 아예 읽지 않았다)
- `[6수01-11]` 분수의 나눗셈 — **100% 무력**
- `[6수01-12]` 분수와 소수의 관계 — **100% 무력**. `0.5`가 정답인데 `5.0`도 통과했다
- `[6수01-04]` 약수 — 역순 답이 통과
- `[2수01-01]` 10개씩 묶음과 낱개 — 자리 숫자만 봐서 `99` 대신 `199`도 통과
- `[2국02-02]` 띄어 읽기 — `∨` 앞 어절만 봐서 `갑니다X`도 통과 (80/120)

**LLM에 생성기 저작을 맡기기 전에 이 게이트가 반드시 있어야 한다.** `generate`와 `verify`를
같은 모델이 같은 오해로 쓰면 하네스는 통과하고 문항은 틀린 채로 나간다.

### 검산이 원리적으로 못 닿는 곳 — 사실 표

정답이 **규칙이 아니라 사실**인 문항이 있다(높임말·반대말·방언·관용 표현·맞춤법·문장 분류).
`verify`가 같은 표를 되읽으므로 **표의 항목이 틀리면 검산도 뮤테이션도 원리적으로 못 잡는다.**

실제로 `'자다' → '자시다'`로 적어 두어 **정답이 선택지에 없는 문항이 나가고 있었다.**
'자시다'는 '먹다'의 높임말이고 '자다'의 높임은 '주무시다'다. 3차 외부 검토가 잡았다.

`check-fact-tables`(9번)가 구조는 본다 — 한 답이 두 표제어에 걸치는지, 표제어와 답이 같은지,
답이 다른 항목의 표제어로도 쓰이는지. 표 6종 114짝, 구조 문제 0.

**그러나 이 게이트는 `자시다` 오류를 잡지 못한다.** 오류를 되넣고 돌려서 확인했다 — 구조는
멀쩡하고 내용만 틀렸기 때문에 exit 0이었다. **사실 표의 내용은 사람이 봐야 한다.**
§12-B가 이 영역이다.

### 채점 계약

`answer.accepts`가 허용 답안의 정의다. `50 × 3.14 = 157`을 `157.00`으로 표시하면 학생이 `157`이라
쓴 정답이 **오답 처리된다**. `check-trim`이 이걸 막는다(12,000건 검사, 잔여 0).

### 부동소수점 금지

`4.7 - 1.9 = 2.8000000000000003`. 소수는 `units/10^scale` 정수쌍으로 들고 표시할 때만 소수점을
꽂는다. 원주율도 `314/100` 정수다. `src/engine/rational.mjs`.

---

## 6. 한국어 표기 — 규칙으로 판정되는 것만 검사한다

문체·자연스러움은 판정하지 않는다. 문항 발문은 기계가 조립하므로 규칙으로 족하다.

조사는 **표기가 아니라 읽는 소리**가 정한다. `27`은 "이십칠"이라 `27은`, `52`는 "오십이"라 `52는`.
분수 `11/8`은 "팔분의 십일"로 분자를 마지막에 읽으므로 `11/8은`. 단위 `1kg`은 "킬로그램"의
"램"에 받침이 있어 `1kg은`.

**`로/으로`는 2항 판정이 안 된다.** 받침 없음과 받침 ㄹ은 `로`, 그 밖 받침은 `으로`다
(`1로`는 "일"이 ㄹ로 끝나 맞고 `3으로`는 "삼"이 ㅁ이라 `으로`). 이 규칙을 검사기에 넣지
않아서 `1 : 3로`가 문면 62,255건 "위반 0"을 통과했다. 넣자 **222건**이 드러났다.

첫 실행에 위반 656건이 나왔다. 눈으로 잡은 3건은 빙산의 일각이었다.

**발문을 LLM에 윤문시키지 않는다.** `42 + 48을 계산하시오`를 다듬다가 숫자가 바뀌면 검증된
정답이 무의미해진다. 윤문이 필요한 건 자산 텍스트(지문) 쪽이다.

---

## 7. 선언으로 한계를 드러내는 패턴 — 이 저장소의 일관된 방식

숨기지 말고 선언하게 만든다. 다섯 곳에 같은 패턴이 있다.

| 무엇 | 선언 | 게이트가 확인하는 것 |
|---|---|---|
| 자동채점 가능성 | `MANUAL_SCORING`(49) / `PARTIAL_SCORING`(8) | manual은 분모에서 제외하되 이유를 남긴다. partial은 분모에 넣고 `scored`/`notScored`를 나눠 적는다 |
| 난이도 축 | `difficultyAxis: 'numeric' \| 'categorical' \| 'single'` + `difficultyNote` | numeric은 계산 크기가 실제로 커지는지 측정. single은 난이도로 결과가 갈리지 않는지 런타임 확인. categorical은 노트가 말한 대로 문항이 갈리는지 |
| 문항 용량 | `capacityNote` (14건) | 포화 상한이 낮은데 이유를 밝히지 않으면 실패 |
| 의도적 비표기 | `nonWords` | 맞춤법 오답 선택지(`꼬치`·`조타`)를 어휘 목록에 넣지 않고 검사에서 뺀다 |
| 교과 전략 | `SUBJECT_STRATEGY` | `parametric` / `mixed` / `asset-required` + `blockedBy` 선행 자산 목록 |

### PARTIAL이 왜 세 교과에 다 있어야 하나

3차 검토가 `PARTIAL_SCORING`이 수학 전용인 것을 구조 문제로 지적했다. 국어·영어를 auto 아니면
manual로만 갈라서 **잴 수 있는 부분이 있는 기준까지 통째로 자동채점 불가로 밀어 넣었다.**

`[4국03-03]` 의견과 이유가 드러나는 글쓰기 — 쓰기는 불가하지만 **주어진 글에서 의견과 이유를
가려내는 것은 자동채점된다.** partial로 옮기면 분모에 들어온다. 그래서 분모가 196에서
**198로 늘었다.** 커버리지가 69.4%에서 68.7%로 내려갔지만 이게 정확한 값이다.
4차 검토가 `[4국03-02]`(보고 글쓰기 — 절차 배열은 잴 수 있다)를 하나 더 지적해 199가 됐다.

### 용량은 고정 문턱으로 재지 않는다

`고유 문항 100개 미만`은 잘못된 지표다. "개념이 작다"와 "덜 썼다"를 구분하지 못한다.
`대각선` 5개는 사각형~팔각형이 전부고, `100까지의 수 쓰기` 90개는 범위의 상한이고,
`곱셈구구` 68개는 81개의 84%다. 표본을 두 배로 늘려도 안 늘면 상한이고, **상한이 낮은 건
결함이 아니라 왜 낮은지 안 밝힌 게** 결함이다.

---

## 8. 계약 명세

### 생성기 계약 (`src/engine/registry.mjs`가 등록 시 검사)

```js
{
  id: 'math.g12.no.s06.add',            // 필수, 고유
  standardCode: '[2수01-06]',            // 필수, /^\[\d[가-힣]+\d{2}-\d{2}\]$/
  skill: '두 자리 수의 덧셈',              // 필수
  format: 'short-answer',                // 아래 형식 중 하나
  difficultyAxis: 'categorical',         // 기본 'numeric'
  difficultyNote: '난이도 1은 받아올림 없음…',  // categorical 이면 필수
  difficulties: [1],                     // single 이면 [1]
  capacityNote: '…셋이 전부다',            // 용량 상한이 낮으면 필수
  generate(rng, { difficulty, standard }) { /* → raw item */ },
  verify(params, answer) { /* → boolean, 독립 경로 */ },
}
```

**등록 거부 조건:** `id` 없음 · `standardCode` 형식 오류 · `skill` 없음 · `generate` 없음 ·
**`verify` 없음** · `difficultyAxis` 값 오류 · `difficulties` 비었거나 1·2·3 밖 ·
`single`인데 난이도 여러 개 · 난이도 하나인데 `single` 미선언 ·
`categorical`인데 `difficultyNote` 없음 · `capacityNote` 빈 문자열

### 문항 계약 (`src/engine/item.mjs`의 `validateItem`)

형식 7종: `short-answer` `fill-blank` `multiple-choice` `compare` `ordering`
`write-expression` `construction`(사람 채점)

**검증 실패 조건:** 알 수 없는 format · stem이 문자열 아님 ·
**stem이 비었는데 그림도 없음** · difficulty 1..3 밖 · `answer.display` 비었음 ·
사람 채점인데 `answer.rubric` 없음/빈 기준 · `answer.accepts` 비었음/빈 값 ·
`solution` 비었음/빈 단계 · `nonWords` 비었음/빈 값/**정답이 `nonWords`에 포함** ·
알 수 없는 `figure.kind` · `figure.spec` 없음 · **`figure.altText` 없음** ·
**`figure.prompt.ko` 없음** · 선택형인데 선택지 3개 미만 · 정답 선택지가 정확히 1개 아님 ·
선택지 텍스트 중복 · 선택형 아닌데 `choices` 있음

### 도형은 SVG로 결정적으로 그린다

이미지 생성 모델을 쓰지 않는다. 자매 저장소 `gongnyang-prompt-kit`을 검토했고 결론은
**도형 도해에 못 쓴다**였다. 그 킷의 교리가 `bar filled to roughly 38 percent`,
"타이포가 정확도를, 도형이 직관을 맡는다"인데 수학은 47°가 **정확히** 47°여야 한다.
킷의 철칙 #9는 `SVG/HTML 합성 일절 금지`인데 수학 도해는 바로 그 SVG가 맞다.

킷은 삽화·표지·캐릭터 레인으로 분리했다. `figure.prompt.ko`는 그 자리를 위해 남겨 뒀다.

렌더러 15종: `measure.clock` `measure.length` `geometry.plane-shape` `geometry.solid-shape`
`geometry.angle` `geometry.line` `geometry.circle` `geometry.triangle` `geometry.quadrilateral`
`geometry.grid-area` `geometry.symmetry` `array.bundles` `data.table` `data.picture-graph`
`data.bar-graph`

한글 음절 11,172자 전량 분해→조합 왕복 검증 통과 (`src/engine/hangul.mjs`).

---

## 9. 선수 관계 그래프

상위 그래프를 못 쓴다고 판정했으므로 새로 저작했다. `src/curriculum/prerequisites.mjs`.

```
수학 121/121 성취기준 · 간선 158개 · 학년군 넘김 54개 (상위는 8개)
순환 0 · 허깨비 참조 0 · 학년군 역행 0
가장 깊은 기준·최대 병목: 검토표에 있다(아래 링크). 간선을 고칠 때마다 낡으므로
이 문서에 옮겨 적지 않는다 — 같은 부류의 낡은 수치가 두 번 재발했다(§16).
```

`POST /v1/remediation`이 고리를 닫는다. 채점 → 취약 성취기준 → 선수 역추적 → 학습 순서로
배치한 복습 학습지. 취약 기준을 반복시키지 않는다. 두 자리 덧셈이 안 되는데 분수의 덧셈을
반복시키면 같은 자리에서 계속 막힌다.

### 3차 검토가 여기서 실제 결함을 잡았다

"분수의 나눗셈 조상에 자연수 나눗셈 전무"는 문자로는 틀렸다 — 조상에는 있었다. 그런데
`[4수01-07]`까지 **6홉**이고 경로가 약분을 우회했다.

```
[6수01-11] ← [6수01-10] ← [6수01-09] ← [6수01-06] ← [6수01-05] ← [6수01-04] ← [4수01-07]
```

`remediation` 기본 `depth 2`에서 도달하지 않았고 `[6수01-11]`은 depth 5에서도 미도달이었다.
즉 "분수의 나눗셈이 막혔다"고 판정해도 자연수 나눗셈을 복습에 넣지 못했다. **지적이 맞다.**
간선 4개를 직접 연결하고 과함 1건을 교체했다(154 → 158).

**검토표: [`docs/review/prerequisites.md`](docs/review/prerequisites.md)** — 간선 158개를 학습
순서로 정렬하고 양끝 기준의 소주제·학년군을 붙였다. `넘김` 열이 영역·학년군을 넘는 간선을 표시한다.

---

## 10. API

```
GET  /health
GET  /v1/subjects
GET  /v1/standards?subject=math&grade=1-2&covered=true
GET  /v1/generators
GET  /v1/coverage
GET  /v1/prerequisites?code=[2수01-06]
POST /v1/worksheets   {seed, subject, grade, domain, count, difficulty, includeAnswers}
POST /v1/items        {code, count, difficulty, seed}
POST /v1/grade        {seed, ...같은옵션, responses, manualEvaluations}  → 채점 + responseRecords
POST /v1/accuracy     {records:[...]}  교사 인증 · 누적 응답 → 정답률·난이도 역전
POST /v1/remediation  {weakStandards:["[6수01-08]"], depth, count}
```

기본값으로 학습자용 응답에서 정답·풀이·선택지 `correct` 플래그를 제거한다.
`includeAnswers=true`가 교사용이다. 작도 문항은 정확도 계산에서 분리해 `manualScoring`으로 넘긴다.

`/v1/accuracy`는 교사 인증을 요구하고 저장하지 않는다 — 누적은 호출자 몫이다.
값을 자동으로 바꾸지도 않는다.
표본 30 미만이면 `accuracy`가 `null`이다. 세 번 풀어 두 번 맞은 것을 0.67이라고 부르면
숫자가 사실보다 세 보인다. 실측 확인: 응답 240건에서 일부러 뒤집은
`math.g12.no.s06.add`의 `d1 0.5682 → d2 0.7321` 역전을 격차 0.1639로 잡아냈다.

---

## 11. 파일 지도

```
src/ontology/     상위 데이터 읽기 전용 접근. source(무결성) · spine(정규화) · audit(품질측정)
src/curriculum/   교육과정 지식. prerequisites · scoring-policy · units
                  korean-vocab · korean-sentences · english-vocab
src/engine/       rng(시드 결정적) · item(계약) · registry(생성기 등록·커버리지) · worksheet(조립)
                  rational(정수 유리수·고정소수점) · hangul(자모 계산) · korean-number(조사)
                  response-log(정답률 집계·난이도 역전 탐지)
src/generators/   math/ 9파일 147개 · korean/ 2파일 22개 · english/ 1파일 6개
                  corpus(문장제 공용 어휘 — 이름·사물·단위명사)
src/render/       svg-base · figure-svg(디스패치) · figure-geometry34 · worksheet-text
src/server/       app(라우터) · grade(무상태 채점)
bin/              게이트 3종 (build-spine · audit-ontology · verify-generators)
                  + serve · worksheet
tools/            게이트 15종 (check-boundaries · mutation-test · check-korean
                  · check-prerequisites · check-vocabulary · check-fact-tables
                  · check-difficulty · check-difficulty-notes · check-capacity
                  · check-trim · check-review-doc · check-vocab-selfapproval
                  · check-asset-requirements · check-coverage-schema
                  · build-english-official-vocabulary --check)
                  + 도구 6종 (export-review-tables · export-asset-tables · export-math-tables
                  · sample-items · figure-gallery · shot)
data/             spine · curriculum · coverage · audit (모두 생성물, 커밋됨)
docs/review/      전문가 검토용 표 (생성물). math.md · assets.md · prerequisites.md · english.md
```

`data/*.json`은 전부 `schema` 필드를 갖는다. 게이트가 생성하며 손으로 고치지 않는다.

---

## 12. 검토자에게 묻고 싶은 것

사실이 아니라 **판단**을 검토해 달라. 우선순위 순이다.
**A와 B가 이번 회차의 핵심이다** — 게이트가 원리적으로 못 닿는 영역이다.

### A. 영어 재분류 결과가 맞는가 (4차에서 5건 확정, 잔여 판정 요청)

4차 검토가 5건을 확정했다 — `[4영01-04]`·`[6영01-02]` 소리 내어 읽기(음성 산출),
`[4영01-09]`·`[6영01-09]` 공감하며 듣기(태도), `[6영02-06]` 감정·의견 표현(개방 산출).
모두 국어 선례와 같은 분류이고 실측으로 대조했다. manual 10 → 15, 분모 30 → 25.

`[4영02-05]`(자신과 주변 소개)·`[4영02-07]`(자신의 감정)은 **옮기지 않았다.** 대신
`blockedBy`에 "표현 문항은 그림·실물을 제시해 답을 제약하는 설계가 선행 조건"임을 못 박았다.
이 설계가 없으면 개방 산출이 되어 재분류 대상이 된다. **이 판정이 맞는가?**
남은 22개 중 더 옮길 것이 있는가?

**판정표: [`docs/review/english.md`](docs/review/english.md)** — 40개 전체의 성취기준 내용과
현재 분류가 표로 있다. 판정 대상이 표시돼 있고 개수는 표의 요약이 기준이다(문서에 옮겨
적으면 낡는다 — 두 번 겪었다). 저장소를 파지 않고 표만 보면 된다.

### B. 저작자가 지은 자산의 내용이 맞는가 — 게이트가 못 닿는 영역

**이게 가장 위험한 곳이다.** `verify`가 같은 표를 되읽으므로 표가 틀리면 검산도 뮤테이션도
못 잡는다. 3차 검토가 `'자다'→'자시다'`를, 4차 검토가 9건을 잡은 것이 그 증거다.

**검토표: [`docs/review/assets.md`](docs/review/assets.md)** — 저작 자산 전 종의 내용
전량과 판정 근거가 있다(종수·항목 수는 표의 합계가 기준이다). 각 절에 **무엇을 판정해야
하는지**와 **틀리면 문항에 무슨 일이 생기는지**를 적었다. 소스를 파지 않고 표만 보면 된다.
**5차 수리에서 저작한 14절은 아직 아무도 검토하지 않았다 — 최우선이다.**

전 판본은 개수만 표로 정리하고 내용은 소스 3파일에 흩어 뒀다. 선수 관계는 검토표로 뽑아
줬는데 자산은 안 뽑았다 — "검토 요청과 검토 가능한 산출물은 다르다"는 1차 검토의 지적이
여기서 반복됐다.

**4차 수리로 새로 들어온 것을 특히 봐 달라.** 아직 아무도 검토하지 않았다.

| 무엇 | 왜 위험한가 |
|---|---|
| 방언 4건 교체 (감자/감재 · 바다/바당 · 김치/짐치 · 어머니/어멍) | 그중 둘은 **저작자가 스스로 검증하지 못해** 교체한 것이다 |
| 띄어 읽기 `허용 자리` 8항목 | 여기 없는 자리는 오답 선택지로 나간다. 빠뜨리면 옳은 답을 틀렸다고 가르친다 |
| 영어 감탄문 3건 (`What a picture!` 등) | 어휘 목록에 형용사가 없어 명사만 쓰는 꼴로 지었다. **영어로 자연스러운가?** |
| 관용 표현 2건 | 뜻풀이 수정 1, 초등 부적합 교체 1 |
| 자산 요구 전량 (`asset-requirements.mjs`) | `sentence` 분류가 정말 지문 없이 성립하는가 — **그것이 다음에 열 대상이다** |

### C. 임시방편으로 넣은 목록 두 개가 옳은가

둘 다 일반 규칙으로 다룰 수 없어 목록으로 처리했다. 목록이 맞는지, 빠진 게 있는지 봐 달라.

- `ONE_SYLLABLE_NOUNS` (`korean-sentences.mjs`) — 조사를 떼면 한 글자로 남는 낱말.
  `'비가'`에서 `'가'`를 떼면 `'비'`가 남는데, 한 글자 어간은 조사가 붙은 어절과 구별할 방법이
  없다. 목록에 없으면 어절이 그대로 어휘 목록에 들어가 `'비가가'`가 만들어진다.
- `TRAILING_PARTICLES` (같은 파일) — 어절 끝에서 뗄 조사. 복합 조사('에는')를 포함해
  긴 것부터 뗀다. '-듯이'는 어미라 떼지 않고 어절을 통째로 둔다.

### D. 선수 관계 158개 간선

교육과정상 틀린 간선이 있는가? 특히 영역을 넘는 54개. **3차 검토에서 새로 넣은 4개**를
집중해서 봐 달라.

- `[6수01-10]` 분수÷자연수 ← `[4수01-07]` 자연수 나눗셈
- `[6수01-11]` 분수의 나눗셈 ← `[4수01-05]` 곱셈과 나눗셈의 관계
- `[6수03-18]` 부피 ← `[4수01-04]` 세 자리 수 범위의 곱셈
- `[6수02-04]` 비례식 ← `[6수01-05]` 최대공약수와 최소공배수

그리고 교체한 1건: `[6수04-04]` 가능성 말 표현의 선수를 `[4수04-03]` 막대그래프 해석에서
`[2수04-01]` 분류하여 세기로 바꿨다. 이게 맞나?

### E. `PARTIAL_SCORING` 8건의 경계가 맞는가

`scored`와 `notScored`를 나눈 선이 타당한가? 예: `[6국03-05]` 고쳐쓰기에서 "맞춤법·띄어쓰기가
틀린 문장을 바르게 고치는 문항"은 재고 "글 전체의 흐름을 고쳐 쓰는 활동"은 안 잰다고 했다.
partial로 둘 수 있는 기준이 더 있는가? 반대로 partial이 과한 게 있는가?

### F. 수학 문항 152개 — **다섯 회차 동안 한 번도 검토받지 않았다**

**검토표: [`docs/review/math.md`](docs/review/math.md)** — 성취기준 121개 · 생성기 152개 ·
표본 **423문항**. 발문·선택지·정답·풀이가 전부 있다.

1~5차 검토가 전부 국어·영어 자산을 팠다. 수학은 초기에 `118/120`이 되고 나서 검토 대상에서
빠졌다 — 자산 검토표 264항목에 수학 생성기 **152개 중 0개**가 올라 있다.
형태가 달라서 넘어갔다. 국어·영어는 문장 목록이라 표로 그대로 뽑히는데 수학은 파라메트릭
생성기라 "자산"이 없다. 그래서 표를 못 만들고 §12-F에 "명백한 오류가 보이면 알려 달라"는
한 줄만 남았다. **커버리지 100%가 검토 완료를 뜻하지 않는다.**

게이트가 이미 보장하는 것은 다시 볼 필요 없다 — 답이 맞는지(뮤테이션 21,603개 통과 0),
조사·표기(문면 73,676건 위반 0), 난이도가 커지는지, 용량·중복·표기 다듬기.

**사람이 봐야 하는 것 넷:**

1. **학년 수준.** 3~4학년 문항에 5~6학년 개념이 섞이지 않았는가.
   이미 겪었다 — 3~4학년에 5학년 약분, `[2수03-01]` 선택지에 정육면체, km→mm 백만 배 환산.
2. **성취기준과 문항의 일치.** 코드는 맞는데 다른 것을 묻고 있지 않은가.
   **저작자는 성취기준 원문을 본 적이 없다.** 상위 저장소가 저작권상 원문을 담지 않아
   코드·학년군·영역·소주제만 대조하고 내용은 저작자 지식으로 만들었다.
3. **오개념.** 이미 겪었다 — 대칭축 문항이 정다각형만 다뤄 "대칭축 수 = 변의 수"를 심고
   있었고, **검산기도 같은 오개념을 전제해** 반례 168건을 오답 처리했다.
4. **풀이 절차**가 교실에서 가르치는 방식과 맞는가.

### G. 게이트 자체의 오탐·미탐 위험

옳은 코드를 위반이라 부르는 게이트는 결국 꺼진다. §13에 겪은 사례 다섯이 있다.
남은 위험이 있는가? 특히 3차 검토가 지적한 부류 — **검사기가 검사하지 않는 것**.

---

## 13. 이미 겪은 함정 — 반복하지 말 것

### 게이트가 옳은 코드를 위반이라 부르면 꺼진다

여섯 번 겪고 여섯 번 검사기를 고쳤다. **생성기를 고치는 게 아니라 검사기를 정밀하게 만들었다.**

- 난이도 축을 소스에서 `difficulty ===`로 찾는 정적 검사를 만들었더니, 생성기를 팩토리 함수로
  만드는 파일에서 블록 경계를 잘못 잡아 선언하지도 않은 생성기를 위반으로 보고했다.
  → 같은 시드로 난이도만 바꿔 직접 호출하는 **런타임 검사**로 교체.
- 조사 검사가 `1/4은`을 틀렸다고 했다. 끝 숫자 `4`만 보고 `는`을 요구했는데 분수는
  "사분의 **일**"이라 `은`이 맞다. → 분수는 분자 읽기로 판정.
- 뮤테이션이 마침표를 지운 문장을 오답 취급했다. 그건 `accepts`에 있는 허용 답안이다.
  → `accepts`에 있는 값은 변형 후보에서 제외.
- 어휘 검사가 `고양이가`·`께서`를 목록 밖 낱말이라 했다. 어절과 조사는 낱말이 아니다.
  → 조사를 떼고 남은 낱말을 본다.
- 난이도 노트 검사에 "넓힘 포함관계" 검사를 넣었더니 두 번 오탐을 냈다. `dedupeKey`에 난이도를
  넣은 생성기가 있어 집합이 원리적으로 겹치지 않고, 노트 문면에서 '넓힘'과 '교체'를 가려낼
  수 없었다(`이상은`은 순수 문법). → **그 검사를 버렸다.** 버린 이유를 코드에 남겼다.
- 조사 중복 검사가 `가을을`(낱말 '가을' + 조사 '을')을 '을을' 중복으로 오탐했다. '세로로'와
  같은 부류인데 '로'처럼 목록에서 빼면 진짜 중복('공을을')을 놓친다. → 겹침 앞부분이
  학년군 어휘의 낱말이면 정상으로 본다. C 수리(어휘 목록 정화)로 '가을'이 뽑히기 시작하며
  드러났다 — **한 수리가 다른 게이트의 잠복 오탐을 깨울 수 있다.**

### "완료"라고 말한 뒤에 발견된 것들

눈으로 검토한 것은 반드시 샌다. 게이트로 만든 것만 남는다.

- 조사 3건을 눈으로 잡고 고쳤다고 커밋 → 실제로는 **268건**이 남아 있었다.
- 176,400문항 "검산 0실패"로 커밋 → 검산 5개가 **아무것도 검증하지 않고** 있었다.
- 미사용 상수를 지우고 커밋 → 사용부를 남겨 `renderWorksheet`가 **ReferenceError로 죽어** 있었다.
  문항 생성 검산은 렌더를 거치지 않으므로 하네스도 못 잡았다.
- `check-difficulty`를 **`verify` 체인에 등록하지 않은 채** "열두 게이트 통과"라고 두 번 커밋했다.
- `verify` 결과를 **`grep` 파이프의 종료코드로 읽고** 있었다. grep이 성공하면 0이라 게이트
  실패를 못 봤다. `npm run verify`를 직접 실행해 종료코드를 봐야 한다.

### 검사기가 검사하지 않는 것을 3차 검토가 잡았다

`check-review-doc`가 `tools/` 파일 수만 세고 **`verify` 체인 등록 여부는 보지 않았다.**
체인에서 게이트를 지워도 파일 수는 그대로라 통과하고, 그 게이트가 돌지 않으니 낡은
`data/audit/*.json`이 수치 대조까지 계속 만족시킨다. 실험으로 재현한 뒤 고쳤다 —
지금은 체인 등록·게이트 총수까지 대조하고, 게이트를 지우면 exit 1이다.

### 문항이 깨려는 오개념을 검산기가 전제할 수 있다

대칭축 문항이 정다각형만 다뤄 "대칭축 수 = 변의 수"라는 오개념을 심고 있었다. 반례
(직사각형 변 4·축 2, 이등변삼각형 축 1)를 넣자 **기존 검산기가 옳은 답 168건을 틀렸다고 보고했다.**
검산기도 같은 오개념을 전제하고 있었기 때문이다.

### 한국어 활용을 문자열 포함으로 되짚지 말 것

`'착하다'`의 어간을 잘라 문장에 있는지 보려 했는데 문장은 `'착합니다'`로 활용돼 있었다.
쓰는 문항을 고르는 문항으로 바꿨다.

### 거부 표집은 영구 불가능해질 수 있다

`받아올림이 생기는 b`를 `rng.until`로 찾았는데 `a`가 0으로 끝나면 그런 `b`가 존재하지 않는다.
자릿수를 직접 구성하는 방식으로 바꿨다.

### 개발 환경 주의

- 셸이 **Git Bash**다. `node -e "...=>..."`의 `>`를 리다이렉트로 먹어 `console.log('OK'))` 같은
  쓰레기 파일이 생기고 코드가 깨진다. 이 세션에서 세 번 만들었고 두 번은 커밋까지 됐다.
  **스크립트 파일로만 실행할 것.**
- 중첩 백틱·템플릿 리터럴이 들어간 heredoc은 멈춘다. 패치 스크립트는 파일로 쓸 것.
- 정규식으로 소스를 편집하다 `MANUAL_SCORING` 선언 전체를 삼킨 적이 있다. 줄 범위를 직접
  지정하는 편이 안전하다.
- Windows 체크아웃이 CRLF다. 상위 manifest 해시는 LF 기준이라 정규화 후 비교한다.
- Chrome을 `127.0.0.1:9222`에 CDP로 열어 두면 `tools/shot.mjs`가 도형 갤러리를 실제 렌더해
  눈으로 확인할 수 있다. 검증 도구이고 산출물이 아니다.

---

## 14. 아직 안 된 것

### 자산이 필요해서 막힌 것 — 이게 남은 전부다

**자산 없이 열 수 있는 것은 거의 다 열었다.**

전 판본은 "`coverage.json`의 `blockedBy`에 무엇이 필요한지 적혀 있다"고 했는데 **거짓이었다.**
미충족 55개 전부가 비어 있었고 `registry`에 그 필드가 아예 없었다. 검토자가 그 지시를
따라가면 아무것도 못 본다. `src/curriculum/asset-requirements.mjs`를 만들어 성취기준마다
조달 성격과 필요 자산을 적었고, 9번 게이트가 누락을 잡는다.

조달 성격 — 비용이 다르므로 뭉뚱그리지 않는다. **성격별 개수는
[`docs/review/assets.md`](docs/review/assets.md) 자산 요구 절이 기준이다**(게이트가 재생성한다.
문서에 옮겨 적으면 낡는다 — §16의 파생 수치 재발 부류).

| 성격 | 무엇인가 |
|---|---|
| `passage` | 지문. **저작권 판단이 선행한다.** 가장 비싸다 |
| `sentence` | 짧은 문장 목록. 저작 가능, 저작권 정리 불필요. **가장 싸다** |
| `media` | 매체 화면·그림 자료 |
| `audio` | 음성. 녹음 또는 합성. 엔진 밖 자산 |
| `dialogue` | 짧은 대화. 화자·맥락 설계 필요 |
| `wordlist` | 공식 어휘 목록(2022 개정 별표) |
| `procedure` | 절차 단계 목록. 저작 가능 |

**`sentence`·`procedure`는 5차 수리에서 전부 열렸다.** 남은 미충족은 조달이 필요한
것뿐이다 — 지문(저작권 판단 선행), 음성, 그림·매체 화면, 공식 어휘 목록.
성취기준별 목록은 검토표의 자산 요구 절이 기준이다.

### 문장 자산과 지문 자산은 비용이 다르다

이 구분을 하지 않으면 투자 우선순위를 정할 수 없다. 위 표의 `sentence`·`procedure` 11개가
지문 자산을 기다리지 않고 열 수 있는 것이다. 그중 3개를 이미 열었다
(사실·의견, 비유, 띄어 읽기).

전 판본은 이 목록을 `SENTENCE_LEVEL_CANDIDATES`로 따로 들고 있었다. `asset-requirements.mjs`의
`kind: 'sentence'`가 같은 일을 더 정확히 하므로(미충족 전량을 덮고 필요 자산까지 적으며
게이트가 낡음을 잡는다) 지웠다. **같은 정보를 두 곳에 두면 한쪽이 낡는다** — 이미 세 번 겪었다.

5차 수리에서 자산 없이 열 수 있는 것을 전부 열었다 — 문장·절차·대화 자산 14개 성취기준
(국어 7 · 영어 7). 남은 미충족은 전부 조달이 필요한 것(지문·음성·그림·공식 목록)이다.

`[6국02-02]` 생략·함축 추론은 `dialogue`이고 **복수 해석 위험이 가장 높아 3차 검토가
최후순위로 조정했다.** 이번에도 열지 않았다.

### 어휘 목록이 아직 시드다

국어 251/430/571, 영어 30/54. 이 저장소가 지은 것이고 공식 자료가 아니다.
2022 개정 별표로 교체할 자리다. **미룰수록 비싸다** — 지금은 위반 0이 기준선이라 교체 직후
`check-vocabulary`의 신규 위반이 곧 교체 영향의 전량 목록이 된다.

### 실측되지 않은 것

- **난이도가 실제로 어려운지** 모른다. `check-difficulty`는 계산 크기 대리 지표이고 정답률이
  아니다. 집계 경로는 이제 있다(`POST /v1/accuracy`, 역전 탐지 실측 확인) —
  남은 것은 실제 학습자 응답이다.
- **교실 적합성** 미검증. §12-F 참조.

### 범위에서 영구 제외

PDF·인쇄 레이아웃·DOM·브라우저 렌더. `check-boundaries`가 `src/`·`bin/`에서 금지한다.
필요하면 클라이언트에서 한다. `tools/`는 개발자 확인용이라 예외다.

---

## 15. 하지 말 것

- 렌더·PDF 재진입 — 게이트가 막는 이유가 §14에 있다.
- 국어 지문 자산의 저작권 정리 전에 지문 기반 생성기 착수 — 자산 계약이 먼저다.
- 발문 LLM 윤문 — §6. 숫자가 바뀌면 검증된 정답이 무의미해진다.
- 게이트 문턱 완화로 위반 없애기 — 옳은 코드를 위반이라 부르는 경우만 검사기를 고친다(§13).
- 한국어 활용을 문자열 포함으로 되짚기 — §13.
- `node -e`로 화살표 함수 쓰기 — §13.

---

## 16. 검토 이력

| 회차 | 무엇을 했나 | 결과 |
|---|---|---|
| 1차 (외부) | §2 측정치 재현, 계약·API 대조 | exit 0 재현. 사실 오류 3건 교정(병목 동률 · `corpus.mjs` 지도 누락 · CDP 서술) |
| 1차 재검증 | 교정 3건 실물 확인, 문서 수치 31개 기계 대조 | 교정 전부 사실. **검토자가 놓친 1건 발견** — `게이트 6종`이 실제 8종 |
| 2차 (외부) | 문서 재작성판 검토 | **`check-review-doc`의 구멍 1건** — 체인 등록을 검사하지 않아 게이트를 지워도 통과 |
| 2차 수리 | 구멍을 실험으로 재현하고 체인 등록·총수 대조 추가 | 게이트를 지운 상태에서 exit 1 확인 |
| 3차 (외부) | 검토표 포함 전면 검토 | **언어 오류 2건 실증**(`자시다` · `1 : 3로`) + 미탐 1건(`로/으로`) + 선수 간선 과함 1·누락 후보 3 + `PARTIAL_SCORING` 구조 문제 + 노트↔코드 검사 공백 |
| 3차 수리 | 지적 전량 처리 | 로/으로 도입으로 **222건 추가 적발** → 0. 선수 간선 5건 수정. PARTIAL 세 교과 확장. 게이트 12 → 14 |
| 후속 실행 | 국어 문장 자산 3개 성취기준, `response-log` 배선 | 국어 13/48 → 16/48. 읽기·문학이 처음 0을 벗어남. 죽은 모듈 0개 |
| 4차 (외부) | 자산 내용 전면 검토 + 지시서 작성 | **자산 오류 9건 실증** · 어휘 자기승인 구멍 · 영어 재분류 5건 확정 · §9 파생 수치 재발 |
| 4차 수리 | `docs/fix-directives.md` A~G 전량 처리 | 영어 3/30 → 3/25. 게이트 14 → 15. 커버리지 139/194 (71.6%) |
| 4차 (외부) | §12 A~G 재판정. 자산 표 전수 정독(154짝+문장 51), `sentenceAssetWords()` 실측, 신규 게이트 2종 소스 검토, 새 문항 표본 | exit 0 재현. **C에서 실측 오염 14건/139** · B에서 방언 3건 의심 + 영어 예문 2건 복수 정답 · §9 병목 서술 낡음(2→4 동률) 교정 |
| 4차 수리 (검토자) | C 직접 수리 — 복합 조사('에는')·'처럼'·'같이' 등록, '-듯이' 통짜 유지, 한 글자 낱말 9개 보강. 나머지 항목은 `docs/fix-directives.md` 지시서로 | 오염 14건 → **0건**, 어휘 목록 251/430/571로 정화. 수리가 잠복 오탐을 깨움 — '가을'이 뽑히자 조사 중복 검사가 `가을을`을 오탐, 어휘 인지 방식으로 정밀화(§13 여섯째 사례) |

**세 회차의 성격이 달랐다.** 1차는 문서의 낡은 수치를, 2차는 그 수치를 지키는 장치의 구멍을,
3차는 **게이트가 원리적으로 못 닿는 데이터 내용**을 잡았다. 갈수록 안쪽을 찔렀다.

### 4차 처리 결과

`docs/fix-directives.md`의 A·B·E·F·G를 지시서가 정한 순서로 전량 처리했다.
C(문장 자산 조사 분해 오염)와 조사 중복 검사 정밀화는 4차 검토자가 직접 수리했다.

| 항목 | 무엇을 | 결과 |
|---|---|---|
| B-2 | 관용 표현 2건 | 뜻 수정 1, 부부 맥락 표현 교체 1 |
| B-3 | 영어 예문 복수 정답 2건 | 명령문 → 감탄문. 지시서 예시어가 어휘 목록 밖이라 목록 안 낱말로 다시 지음 |
| B-4 | 높임 표 회귀 금지 | 주체·객체 높임 혼재 제약을 선언부에 주석으로 |
| B-1 | 방언 3건 → **4건** | `감재`를 감자로 고치니 표준어가 중복돼 제주 항목까지 교체 |
| E | `[4국03-02]` PARTIAL 이관 | 국어 분모 48 → 49 |
| A | 영어 5건 재분류 | 영어 3/30 → **3/25**, manual 10 → 15. 국어 선례 5건 실측 대조 |
| F | 띄어 읽기 오답 풀 | `alsoAcceptable` 도입. 8개 중 6개에 허용 자리가 있었다 |
| G-1 | 자기승인 탐지 게이트 | `작년에` 1건 적발. 시제 부사어라 사유와 함께 예외 등록 |
| G-2 | §9 파생 수치 | **검토표 링크로 대체하는 쪽을 택했다** |

**G-2에서 왜 대체를 택했나.** 지시서가 두 선택지를 줬다 — 지우고 링크로 대체하거나,
유지하고 `check-review-doc`의 기계 대조에 넣기. 같은 부류의 낡은 수치가 두 번
재발했으므로(1차 `게이트 6종`, 4차 `병목 2→4 동률`) 원인을 없애는 편을 택했다.
파생 수치를 두 곳에 두면 한쪽이 반드시 낡는다. 검토표는 게이트가 재생성한다.

**B-1이 지적 3건에서 4건이 된 이유.** `고구마/감재`를 `감자/감재`로 고치니 표준어
`감자`가 두 번 나왔다. 난이도 2 이상은 "표준어 → 방언"을 거꾸로 묻기 때문에 정답이
둘이 된다. `check-fact-tables`는 방언 → 표준어 방향만 보므로 이 중복을 잡지 못한다.
지시서가 미리 경고해 둔 함정이었다.

**4차에 바라는 것:** §12-A(영어 재분류)와 §12-B(저작자가 지은 자산의 내용)다. B는 게이트가
못 닿는 영역이고 3차에서 `자시다` 하나가 나왔으니 더 있을 것이다. 자산 목록과 개수를 §12-B
표에 정리해 뒀다.

### 4차 판정 — §12 A~G (2026-08-05, 미수리)

- **A. 3차 판정 유지, 대상 27개 기준 확정판.** 원리적 불가로 옮길 것 5개 —
  `[4영01-04]` `[6영01-02]` 소리 내어 읽기 → speech-production(국어 `[2국02-01]` 선례),
  `[4영01-09]` `[6영01-09]` 공감하며 듣기 → disposition(`[2국05-04]` `[4국05-05]` 선례),
  `[6영02-06]` 자신의 감정·의견·경험·계획 → open-production(`[2국03-02]` 선례).
  `[4영02-05]`(자신 소개) `[4영02-07]`(자신의 감정)은 그림 제시로 제약하는 설계를 조건으로
  자산 대기 유지 — 어기면 개방 산출이 된다. 적용 시 영어 3/30 → **3/25**, manual 10 → 15.
- **B. 자산 154짝 + 문장 51개 전수 정독.** 높임 14 · 반대말 20 · 맞춤법 18 · 포함 관계 10 ·
  사실·의견 16 · 비유 14 · 띄어 읽기 8 — **전부 옳다**(띄어 읽기 3기준은 초등 지도 관행과 부합).
  적발·권고 6건:
  1. **방언 `고구마→감재(강원)` 의심** — 강원 '감재'는 통상 **감자**다(제주 항목이 감자→지슬로
     이미 옳게 있음). 감저(고구마)와 혼동했을 가능성.
  2. **방언 `많이→많이나(충청)` 근거 박약**, `수건→수군(제주)` 확인 불가 — 방언 3건은
     국립국어원 지역어 자료로 검증하거나 뺄 것. 방언은 지역민에게 사실성이 바로 노출된다.
  3. 관용 `입이 무겁다` 뜻풀이 손질 — '말을 조심해서 옮긴다'는 옮긴다는 뉘앙스다.
     '말을 함부로 옮기지 않는다'가 정확하다.
  4. 관용 `바가지를 긁다` — 뜻은 맞으나 부부 맥락 표현이라 초등 문항으로 교체 권고.
  5. **영어 예문 `Look at the sun!` `Be careful!` 복수 정답** — 명령문은 마침표도 옳다.
     감탄문형(What a…!)만 부호가 확정된다. 교체하거나 accepts에 `.`을 추가할 것.
  6. 높임 표에 주체 높임(주무시다)과 객체 높임(드리다·뵙다·여쭈다)이 섞여 있다 — 문항이
     "높임 표현"으로만 물으면 문제없으나, 오답 풀에 `주시다` 같은 다른 방향 높임이 들어가면
     정답이 둘이 된다. 오답 풀을 이 표 안에서만 뽑는 현재 구조를 유지할 것.
- **C. 실측 오염 14건/139 — 두 목록의 누락이 이미 발현해 있다.** `sentenceAssetWords()` 출력에서
  어절 유입 `공을` `배와` `귤을` `색이` `것이` `볼이` `나는` `가을에`(복합 조사 '에는' 미처리),
  '처럼' 미등록으로 `사과처럼` `솜처럼` `솜사탕처럼`, '같이/듯이'를 '이'로 오분해한 쓰레기 어간
  `새같` `보석같` `속삭이듯`. `ONE_SYLLABLE_NOUNS`에 공·배·귤·색·볼·나·것 누락.
  이 토큰들이 학년군 어휘 목록에 들어가 있고, **자산이 스스로 화이트리스트에 공급하므로 어휘
  게이트는 원리적으로 못 본다**(자기승인 맹점 — G). 자산이 38문장뿐이니 자동 스트립을 버리고
  문장별 낱말을 명시하는 편이 안전하다.
- **D. 신규 4간선 전부 타당, 교체 1건 타당.** 분수 나눗셈에 자연수 나눗셈·곱셈-나눗셈 관계가
  직결됐고 부피·비례식의 산술 조상도 맞다. 부작용: 병목 순위가 바뀌어 §9 서술이 낡아 있었다
  (2개 동률 → 4개 동률, 교정함). 파생 수치 미검사 부류의 재발 — 1차 `게이트 6종`과 같은 급.
- **E. 7건 경계 전부 타당, 과한 것 없음.** 추가 후보 1: `[4국03-02]` 보고 글쓰기 —
  '절차 순서 배열'이 닫힌 하위 능력이고 `ordering` 형식이 이미 있다.
- **F. 새 문항 표본 이상 없음.** 띄어 읽기 문항의 오답 선택지에 '아침에 ∨'(부사어 뒤) 같은
  판정 갈림 위치가 섞이는 것만 주의 — 선언한 3기준 밖 위치는 오답 풀에서 빼는 편이 안전하다.
- **G. 남은 맹점 2.** ① C의 자기승인 맹점 — 어휘 게이트는 화이트리스트 안 토큰의 형태를 보지
  않는다. 조사로 끝나는 토큰 검사를 자산 로드 시점에 넣을 수 있다(단 '아이'·'사과'처럼 조사
  모양으로 끝나는 정상 낱말이 있어 목록 기반이어야 한다). ② §9류 파생 서술은 여전히 게이트
  밖이다 — 병목·최심 기준을 `export-review-tables`가 내는 표에 포함하면 문서가 표를 가리키게
  되어 낡을 수치 자체가 없어진다.

### 5차 판정 — §12 A~G (2026-08-05, 미수리)

16게이트 exit 0 재현. 자산 173항목 + 자산 요구 59항목 전수 정독. 생성기 소스로 교차 확인.

- **A. 유지.** 4차 재분류 5건은 정확히 적용됐고(판정표 재생성 확인: manual 15, 판정 대상 22),
  `[4영02-05]`·`[4영02-07]`을 blockedBy 조건부로 남긴 판정도 옳다. 남은 22개에서 추가로
  옮길 것 없음.
- **B-1. 시제 표 — 이번 회차 최대 적발. 12행 중 7행이 복수 정답 위험이다.**
  한국어 현재형은 예정된 미래를 나타낸다("**내일** 도서관에 **갑니다**"는 규범적으로 옳은
  문장이다). 그래서 미래 4행의 현재형 오답 후보(내일 갑니다 · 다음 주에 만납니다 ·
  모레 떠납니다 · 내년에 입학합니다)는 실제로는 허용되는 표현이다. '오늘'·'이번 주에'는
  과거와도 호응한다("오늘 연습했습니다" 자연스러움). '지금 갈 것입니다'도 갈린다.
  생성기(`korean.g56.gr.s05.tense`)가 이 오답 후보를 선택지로 그대로 내보내는 것을 소스로
  확인했다 — 아이가 문법적으로 옳은 답을 고르고 오답 처리된다. **안전한 행은 과거 부사어
  5행뿐이다.** 수리 방향: 미래 행의 오답은 과거형만, '오늘'·'이번 주에'는 '지금'류로 교체
  하거나 삭제. 검산이 같은 표를 되읽으므로 뮤테이션은 원리적으로 못 잡는 부류다.
- **B-2. 띄어 읽기 허용 자리 비일관 1건.** '나는 **학교에** 갑니다'(부사어 뒤 불허)와
  '꽃이 **활짝** 피었습니다'·'우리는 **운동장에서** 공을 찼습니다'(부사어 뒤 허용)가 같은
  구조인데 판정이 갈린다. 셋 다 허용하거나 셋 다 불허하고, 기준(성분 기준인지 문장 길이
  기준인지)을 자산에 명문화할 것.
- **B-3. 소소한 재고 3건.** ① 높임 명사 4건(진지·연세·성함·댁)의 '주체' 분류 — 문법 범주는
  어휘적 높임(존칭 명사)이다. 제3 분류를 권고. ② 포함 관계 '옷 ⊃ 양말·장갑' — 착용물이지
  옷은 아니다. 교과 관행은 바지·치마류만 옷으로 묶는다. ③ '그릇 ⊃ 냄비' — 조리도구 성격이
  강해 재고. 나머지 자산은 전부 통과 — 방언 12(4차 교체 4건 포함) · 관용 20 · 맞춤법 18 ·
  반대말 20 · 사실·의견 16 · 비유 14 · 문장 성분 16 · 영어 13(What a X! 감탄문형은 영어로
  자연스럽고 부호가 확정된다).
- **C. 유지.** 목록은 현행 자산 기준 완전하다(오염 0 실측은 4차 수리에서 확인). §12-C의
  개수 문구가 낡아 있어 교정했다(18/19 → 27/30). 남은 위험은 새 문장 추가 시 수동 유지보수뿐.
- **D. 유지.** 신규 4간선·교체 1건 모두 4차 판정대로 타당. 재론 없음.
- **E. 8건 전부 타당.** `[4국03-02]` 이관 확인. 과한 것 없음.
- **F. 자산 요구 59항목에서 성취기준-자산 불일치 3건 + 모순 등재 1건.**
  ① `[4영02-03]`(소리-철자 관계로 단어 **쓰기**)의 필요 자산이 "따라 말할 문장 목록" —
  다른 기준([4영02-01] 계열)의 서술이 들어왔다. 받아쓰기용 단어 목록이 맞다.
  ② `[6영02-05]`(장소·위치·행동 순서 **설명**)이 "요약 대상 글과 정답"으로 passage 배정 —
  그림·상황 제시 + 허용 답안 틀(media/sentence)이 맞다. ③ `[6영02-02]`(그림 보고 말하거나
  쓰기)가 wordlist — 그림 자산이 본체이므로 media 재고. ④ **`[4국03-05]`는 모순 등재다** —
  `scoring-policy`에선 disposition(태도, 분모 제외)인데 자산 요구에는 sentence 조달로 있다.
  둘 중 하나가 거짓이다. 부수: `[6국01-01]`·`[6국01-02]`를 글 대화로 열면 듣기 기준을 읽기로
  재는 것이므로 PARTIAL 명시가 정직하다.
- **G. 게이트 공백 1 + 파생 문구 재발 2.** ① `check-asset-requirements`는 미충족 55개가
  전부 선언됐는지만 보고 **선언 59개가 전부 미충족인지는 보지 않는다.** 그 틈으로 F-④의
  모순 등재가 들어왔다(초과 4건). 역방향 검사(선언 ⊆ 미충족∪PARTIAL)를 추가하면 잡힌다.
  ② §12-A "판정 대상 27개"(실제 22)와 §12-C "18개/19개"(실제 27/30)가 낡아 있었다 — 교정함.
  파생 수치를 문서에 손으로 옮겨 적는 한 재발한다. G-2에서 택한 "링크로 대체" 원칙을
  §12 본문의 개수 문구에도 적용할 것.

### 5차 수리 (검토자 직접, 2026-08-05)

판정한 검토자가 직접 수리했다. 언어 판단이 계속 개입되는 항목들이라 판정-수리 분리보다
빠르고 안전했다.

| 항목 | 무엇을 | 결과 |
|---|---|---|
| B-1 시제 표 | **재설계.** 현재 행 3개 삭제(과거·미래 모두와 호응), 미래 행 오답을 과거형·과거 진행형으로, 행별 `frame`(문맥 어절) 도입, d3 추가 오답은 반대 시제에서만 | 12행 → 9행. 복수 정답 경로 제거. 표본 육안 확인 |
| B-1 추가 적발 | 발문이 `도서관에`로 고정돼 있었다 — "내년에 **도서관에** 입학할 것입니다" 같은 어색한 문장이 나가고 있었다. 판정 때는 표만 보고 놓쳤고 **표본을 뽑아 보고서야 보였다** | `frame`으로 해소. 자산 표에 문맥 열 추가 |
| B-2 띄어 읽기 | '학교에 뒤' 허용 추가 — 부사어 뒤 허용 원칙으로 통일, 원칙을 자산 주석에 명문화 | 비일관 해소 |
| B-3 | 높임 종류에 '어휘(명사)' 분류 신설(진지·연세·성함·댁), 옷 ⊃ 바지·치마, 그릇 ⊃ 접시·컵으로 교체 | 검토표 반영 |
| F-①②③ | `[4영02-03]` 서술을 단어 쓰기용으로, `[6영02-05]`·`[6영02-02]`를 media로 재배정 | 잘못 들어온 서술 2건 제거 |
| F-④ | 모순 등재 4건 제거 — `[4국03-05]` `[2국06-01]` `[2국06-02]` `[6국06-03]` (전부 자동채점 불가 분류) | 선언 59 → 55 = 미충족 55 |
| G-① | `check-asset-requirements`에 역방향 검사(`not-openable`) 추가 | **모순 항목을 되넣어 exit 1 확인 후 제거** |
| G-② | §12-A·B·C와 §14의 파생 개수를 검토표 링크로 대체 | 낡을 수치 자체를 제거 |

**시제 표에서 배운 것:** 표만 읽는 판정은 표의 참·거짓은 잡아도 **표가 발문과 만나는
방식의 결함은 못 잡는다.** '도서관에' 고정 문맥은 표에는 없고 생성기에만 있었다.
자산 판정에는 반드시 표본 문항을 함께 볼 것.

### 5차 후속 — 저작 가능 자산 전량 채움 (검토자 직접, 2026-08-05)

조달이 필요 없는 것(문장·절차·대화)을 전부 저작했다. **14개 성취기준이 열렸다** —
국어 7(감각 표현 · 인물 마음 · 의견-이유 · 고쳐쓰기 · 절차 배열 · 대화 추론 · 근거 타당성),
영어 7(문장 뜻 · 단어 쓰기 · 행동 지시 · 소개 묘사 · 예시문 빈칸 · 묻고 답하기 2).
커버리지 139/194 → **150/194 (77.3%)**, 생성기 175 → 193.

- 자산은 전부 판정 갈림을 피해서 지었다 — 마음 낱말은 가까운 말을 섞지 않고 4개 고정,
  묻고-답하기 오답은 짝마다 손으로 선별, 빈칸 오답은 목록 안 낱말 중 문맥에 안 맞는 것만,
  절차는 순서가 상식으로 확정되는 것만. 각 제약을 자산 주석에 남겼다.
- 듣기말하기(`[6국01-01·02]`)와 영어 '말하거나' 계열 4건은 **partial**로 열었다 —
  글 대화·고르기 문항은 듣기·발화 그 자체가 아니다(5차 판정의 원칙).
- 저작 중 게이트 오탐 1건: `check-boundaries`가 영어 예문 `Close the window.`의
  `window.`(문장 부호)를 브라우저 전역으로 오인했다. 식별자가 따라올 때만 위반으로
  정밀화했다(§13 일곱째 사례).
- **새로 지은 자산 전부가 §12-B 검토 대상이다.** 검토표가 재생성됐고, 시제 표의 교훈대로
  표본 문항 육안 확인까지 마쳤다. 남은 미충족 41개는 전부 조달 대상(지문·음성·그림·공식 목록).
